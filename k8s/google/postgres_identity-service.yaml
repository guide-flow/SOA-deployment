apiVersion: v1
kind: Service
metadata:
  name: postgres-identity
  namespace: soa-app
  labels:
    app: postgres-identity
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    io.kompose.service: postgres-identity
  type: ClusterIP
