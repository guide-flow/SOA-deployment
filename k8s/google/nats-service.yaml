apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: soa-app
  labels:
    app: nats
spec:
  type: ClusterIP
  ports:
    - name: client
      port: 4222
      targetPort: 4222
    - name: monitoring
      port: 8222
      targetPort: 8222
  selector:
    io.kompose.service: nats
