apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stakeholders-images
  namespace: soa-app
  labels:
    app: stakeholders
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 1Gi
