apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stakeholders-images
  namespace: soa-app
  labels:
    app: stakeholders
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
