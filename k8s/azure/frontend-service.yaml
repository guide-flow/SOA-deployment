apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: soa-app
  labels:
    app: frontend
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 4200
      targetPort: 80
  selector:
    io.kompose.service: frontend
