apiVersion: v1
kind: Service
metadata:
  name: postgres-stakeholders
  namespace: soa-app
  labels:
    app: postgres-stakeholders
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    io.kompose.service: postgres-stakeholders
  type: ClusterIP
