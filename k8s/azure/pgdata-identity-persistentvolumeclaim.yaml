apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-identity
  namespace: soa-app
  labels:
    app: postgres-identity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
