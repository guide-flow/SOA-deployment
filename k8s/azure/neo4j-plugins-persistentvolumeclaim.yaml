apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neo4j-plugins
  namespace: soa-app
  labels:
    app: neo4j
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
