apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-identity
  namespace: soa-app
  labels:
    app: postgres-identity
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: postgres-identity
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: postgres-identity
        app: postgres-identity
    spec:
      containers:
        - name: postgres-identity
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_DB_IDENTITY
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: POSTGRES_USER_IDENTITY
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: POSTGRES_PASSWORD_IDENTITY
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: pgdata-identity
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U postgres -d identity
            failureThreshold: 5
            periodSeconds: 10
            initialDelaySeconds: 30
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U postgres -d identity
            failureThreshold: 3
            periodSeconds: 5
            initialDelaySeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      restartPolicy: Always
      volumes:
        - name: pgdata-identity
          persistentVolumeClaim:
            claimName: pgdata-identity
