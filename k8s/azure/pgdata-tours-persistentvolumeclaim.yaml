apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-tours
  namespace: soa-app
  labels:
    app: postgres-tours
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
