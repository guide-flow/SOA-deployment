apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-tours
  namespace: soa-app
  labels:
    app: postgres-tours
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
